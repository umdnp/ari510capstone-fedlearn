# Configuration Files

Store configuration files for experiments here:
- YAML/JSON configs for model hyperparameters
- Federated learning settings (number of clients, rounds, etc.)
- Data preprocessing parameters

Example:
- `baseline_config.yaml`
- `federated_config.yaml`
